{
  "name": "nev-climate-data-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "NEV Climate Data — frontend en migration progressive vers React (A3.1/A3.2). Chaque page HTML est migrée un lot à la fois (voir le plan de migration) ; les pages non encore migrées restent en HTML statique + JS simple, servies telles quelles. Tailwind CSS v4.",
  "scripts": {
    "dev": "vite",
    "dev:tailwind": "npx @tailwindcss/cli -i ./src/input.css -o ./src/css/tailwind.css --watch",
    "build": "npm run build:tailwind && npm run build:vite",
    "build:tailwind": "npx @tailwindcss/cli -i ./src/input.css -o ./src/css/tailwind.css --minify",
    "build:vite": "rm -rf assets/react && vite build"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.9",
    "@tailwindcss/vite": "^4.0.9",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.9",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
